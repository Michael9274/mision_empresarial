function calendarClient() {

    var newEvent;
    var editEvent;

    $('#CalendarReserveClient').fullCalendar({
        // put your options and callbacks here
        themeSystem: 'bootstrap3',
        defaultView: 'listWeek',
        header:
            {
                left: 'prev,next today',
                center: 'title',
                right: 'agendaDay,agendaWeek,month'
            },
        hiddenDays: [0],
        bootstrapGlyphicons: {
            prev: 'glyphicon-arrow-left',
            next: 'glyphicon-arrow-right'
        },
        allDaySlot: false,
        minTime: "07:00:00",
        maxTime: "21:00:00",
        businessHours: [
            {
                // days of week. an array of zero-based day of week integers (0=Sunday)
                dow: [1, 2, 3, 4, 5], // Monday - Friday
                start: '07:30', // a start time
                end: '21:00', // an end time
            },
            {
                dow: [6],
                start: '08:00',
                end: '17:00'
            }
        ],
        selectable: true,
        selectHelper: false,
        select: addEvent,
        editable: true,
        eventLimit: true, // allow "more" link when too many events
        navLinks: true, // can click day/week names to navigate views
        eventOrder: "roomNumber",
        slotEventOverlap: false,
        events: 'events',
        views: {
            agenda: {

            }
        },
        eventRender: function(event, element) {
            console.log(event);
            element.find('.fc-time').append('<button type="button" id="deleteEventButton" class="close btn-delete" data-dismiss="modal" aria-label="Close"><span class="delete" aria-hidden="true">&#10006;</span></button>');
            (event.occupants > 1) ?
                element.find('.fc-title')
                    .append("<br/> <span class='occupants'>" + event.occupants + " occupants" + "</span> <br/> <span class='roomNumber'>" + event.roomNumber + "</span>")
                : element.find('.fc-title')
                    .append("<br/> <span class='occupants'>" + event.occupants + " occupant" + "</span> <br/> <span class='roomNumber'>" + event.roomNumber + "</span>")
            element.find("#deleteEventButton").click(function() {
                $('#calendar').fullCalendar('removeEvents',event._id);
            });
        }
    })
}

function calendarConfig() {

    $('#autoGeneratedID').dataTable({});

}